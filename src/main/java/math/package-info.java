/**
 * Matematikai számításokhoz segítséget nyújtó osztályok.
 * 
 * 
 * Egyszerűen lehet vektorműveleteket végezni:
 * <pre>
 * Vector2D a=new Vector2D(10,10);
 * Vector2D b=new Vector2D(6,19);
 * Vector2D newVector=a.multiply(2).add(b).rotate(90).add(new Vector2D(6,7));
 * <pre>
 */
package math;
